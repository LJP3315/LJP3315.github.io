# 2026-05-17 工作记录

## Jekyll 博客 UI 三模块优化（天空蓝 × 毛玻璃 × 浮层卡片）

### 修改文件
1. `_sass/minimal-mistakes/skins/_default.scss`
   - 主色调从灰 #6f777d 改为天空蓝 #4A90D9
   - 注入 `:root` CSS 自定义属性（仅亮色模式）
   - ~~暗色模式 + theme-toggle 已移除（用户要求取消暗色）~~
   - 圆角从 4px 提升至 12px

2. `_sass/minimal-mistakes/_masthead.scss`
   - `position: sticky; top: 0` 导航栏固定
   - `backdrop-filter: blur(20px) saturate(180%)` 毛玻璃效果
   - 滚动后 `.is-scrolled` 投影增强
   - 导航链接 hover 蓝底效果

3. `_includes/masthead.html`
   - ~~亮/暗主题切换按钮已移除~~
   - 保留内嵌 JS：滚动监听（is-scrolled 投影）

4. `_sass/minimal-mistakes/_archive.scss`
   - `.list__item` 添加圆角卡片（14px）、边框、投影
   - 左侧天空蓝渐变色标条（4px）
   - hover 上浮 -4px + 投影增强
   - 颜色变量全部切换为 CSS 自定义属性
   - Grid 卡片同步美化

5. `_sass/minimal-mistakes/_sidebar.scss`
   - 作者卡片整体 card 化（16px 圆角、边框、投影）
   - 头像天空蓝渐变光环（mask-composite 技巧）
   - 作者名天蓝色链接，hover 变深
   - bio 引言用 primary 色斜体展示
   - 社交链接 pill 化：flex 纵向排列，hover 蓝底 + 右移 3px + 图标放大
   - 移除桌面端 opacity 淡入（直接显示）
