---
title: 有关useBean的使用
date: 2026-05-08
categories: 学习记录
tags: JavaEE
---

 PS：实验五 JSP JavaBean 技术相关

#### 文件结构：

```bash
exp05/
├── src/main/java/com/lyon/exp05/
│   └── userBean.java              # JavaBean 数据模型
├── src/main/webapp/
│   ├── index.jsp                  # 首页
│   ├── login.jsp                  # 登录页面
│   ├── register.jsp               # 注册页面
│   ├── treatLogin.jsp             # 登录处理页面
│   ├── treatRegister.jsp          # 注册处理页面
│   ├── successLogin.jsp           # 登录成功页面
│   ├── failLogin.jsp              # 登录失败页面
│   ├── successRegister.jsp        # 注册成功页面
│   └── failRegister.jsp           # 注册失败页面
└── pom.xml                        # Maven 配置文件
```

注意：Java 代码统一存储在 java 文件夹下

---

#### useBean 标签：

语法格式：

```jsp
// id 为 Bean 实例的唯一标识符
// class 为完整的类名
// scope 表示作用域范围
// property="*" 自动匹配表单字段名与 Bean 属性名

<jsp:useBean id="user" class="com.lyon.exp05.userBean" scope="page"/>
<jsp:setProperty name="user" property="*"/>
```

前后对比:

```jsp
传统方法实例：
<%
    String username = request.getParameter("username");
    String password = request.getParameter("password");
    userBean user = new userBean();
    user.setName(username);
    user.setPassword(password);
%>

useBean 方法：
<jsp:useBean id="user" class="com.lyon.exp05.userBean" scope="page"/>
<jsp:setProperty name="user" property="*"/>
```

---

#### 作用域相关：

|   作用域    |   使用场景   |     生命周期     |              实例               |
| :---------: | :----------: | :--------------: | :-----------------------------: |
|    page     | userBean实例 |     当前页面     |  `<jsp:useBean scope="page">`   |
| application |  用户数据库  | 整个应用运行期间 | application.getAttribute("XXX") |
|   session   | 当前登录用户 |     用户会话     |   session.setAttribute("XX")    |
|   request   | 错误信息传递 |     单次请求     |   reqeust.getParameter("XX")    |

---

##### 处理注册的逻辑：

```jsp
<jsp:useBean id="user" class="com.lyon.exp05.userBean" scope="page"/>
<jsp:setProperty name="user" property="*"/>
<%
    // 获取用户输入
    String username = user.getUsername();
    String password = user.getPassword();

    // 使用 application 作用域存储用户数据库（模拟持久化）
    Map<String, String> userDatabase = (Map<String, String>) application.getAttribute("userDatabase");
    if (userDatabase == null) {
        userDatabase = new HashMap<>();
        application.setAttribute("userDatabase", userDatabase);
    }

    // 注册逻辑：验证并保存用户信息

    String message = "";
    String targetPage = "failRegister.jsp";
    if (username == null || username.trim().equals("")) {
        message = "用户名不能为空";
    } else if (password == null || password.trim().equals("")) {
        message = "密码不能为空";
    } else if (userDatabase.containsKey(username)) {
        message = "用户名已存在";
    } else {
        userDatabase.put(username, password);
        message = "注册成功";
        targetPage = "successRegister.jsp";
    }
%>

<jsp:forward page="<%=targetPage%>">
    <jsp:param name="message" value="<%=message%>"/>
</jsp:forward>
```

---

##### 处理注册的逻辑：

```jsp
<jsp:useBean id="user" class="com.lyon.exp05.userBean" scope="page"/>
<jsp:setProperty name="user" property="*"/>
<%
    // 获取用户输入
    String username = user.getUsername();
    String password = user.getPassword();

    // 使用 application 作用域存储用户数据库（模拟持久化）
    Map<String, String> userDatabase = (Map<String, String>) application.getAttribute("userDatabase");
    if (userDatabase == null) {
        userDatabase = new HashMap<>();
        application.setAttribute("userDatabase", userDatabase);
    }

    // 登录逻辑：验证用户名和密码
    String targetPage = "failLogin.jsp";
    String message = "";
    if (username == null || username.trim().equals("")) {
        message = "用户名不能为空";
    } else if (password == null || password.trim().equals("")) {
        message = "密码不能为空";
    } else if (!userDatabase.containsKey(username)) {
        message = "用户不存在";
    } else if (!userDatabase.get(username).equals(password)) {
        message = "密码错误";
    } else {
        message = "登录成功";
        targetPage = "successLogin.jsp";
        session.setAttribute("currentUser", username);
    }
%>

<jsp:forward page="<%=targetPage%>">
    <jsp:param name="message" value="<%=message%>"/>
</jsp:forward>
```

---

#### 总结：

* javaBean 技术可以在一定程度减少代码的书写，增加代码的可复用性
  * 需要注意的是，用于获取的变量名必须与 userBean 类中的变量名完全一致，否则会报空
* 通过使用 HashMap 在 application 作用域存储用户数据，模拟数据库
* javaBean 减少的代码比较有限，主要是获取 user 比较方便，通过类来进行管理
  * setProperty 标签进行自动匹配字段

[实验五完整代码](https://github.com/LJP3315/JavaEE/tree/main/exp05)









